[package]
name = "deadman_switch"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
lettre = { version = "0.10", features = ["smtp-transport"] }
